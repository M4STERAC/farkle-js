export type PlayerList = {
    name: string
    id: number
}

export type PossibleScores = {
    roll: number[],
    score: number
}[]

export type ReturnedScore = {
    roll: number[],
    score: number
}