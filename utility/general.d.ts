export type PlayerList = {
    name: string
    id: number
}

export type CustomScores = {
    roll: number[],
    score: number
}[]

export type ReturnedScore = {
    roll: number[],
    score: number
}